package com.web.study_analysis.study_business.tier;

/**
 * Dùng cho cả khóa học (yêu cầu) và tài khoản (đang có).
 * Khóa PLUS cần user có gói PLUS mới ghi danh & học.
 */
public enum SubscriptionTier {
    FREE,
    PLUS
}
